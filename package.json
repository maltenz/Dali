{
  "name": "dali-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "sst dev",
    "dev:website": "bun --cwd website run dev",
    "dev:cms": "bun --cwd website run dev:cms",
    "build": "bun --cwd website run build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "dependencies": {
    "sst": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
